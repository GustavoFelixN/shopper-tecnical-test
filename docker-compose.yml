services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
      target: base
    volumes:
      - ./server/src:/app/src
      - ./server/nodemon.json:/app/nodemon.json
    expose:
      - '3000'
    ports:
      - '3000:3000'
    command: npm run dev
