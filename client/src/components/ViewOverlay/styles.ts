import styled from "styled-components";

export const StyledViewOverlay = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  width: 33%;
  height: 90vh;
  background-color: rgba(255, 255, 255, 0.8);
  padding: 20px;
  margin: 25px;
  border-radius: 50px;
  z-index: 10;
`;
